[tasks.check]
run = "npm run check"

[tasks.release]
description = "Check and publish the current package version to npm"
depends = ["check"]
raw = true
run = [
  "node scripts/check-release-version.js",
  "npm publish --access public",
]
